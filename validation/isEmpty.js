// Validator isEmpty will only validate empty strings. Below function will be used to check for both empty Objects and Strings.

const isEmpty = value =>
  value === undefined ||
  value === null ||
  (typeof value === 'object' && Object.keys(value).length === 0) ||
  (typeof value === 'string' && value.trim().length === 0);

module.exports = isEmpty;
